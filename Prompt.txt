So a few changes PPH hinges are initially facing downwards, which is wrong. The indicator needs to be on the side with the handles and the hinges need to be facing upwards. Another similar issue is when we are moving the indicator around we need the process dxf button to process the dxf according to where ever we moved the indicator to. Right now it is hard following the rules of corners. When manualy moving the indicator those rules should not apply. There are also times where it seems the rules get switched up. We need to verify that the rules are working the way they should with indicators and their corner rules. We should have the width and height rules overide those rules in certain cases. For example if the height is smaller than the width than the rules for corner indicators need to be flipped. We also need to make sure that its taking into account WJ mins and maxs. I have added some code to the program for detecting Transom Pieces that come over as TRN2 or something similar, I also changed somethings about remakes, full screen, more changes to items in review order on sketches, and such. Review the changes. Next is new additions, Can we add a small UI next to the items that are the sketch? So when you click on the text that has the machine and order number next to it will show up a series of options, increase size, decrease size, delete, and rotate. If possible that would be greate. Another one, Can we add at the top how many pages of the sketch there are? So if there's 3 panels then theres 3 pages and it should say if your own page one its 1/3 and so on. Another one, what we did previously to change the dxf type to AC1032 made it back to where I couldn't open the dxf to edit it or see it. It would open and then it was say press enter to continue, I would press it and then it would open Drawing1.dwg. Doing it this way made the file unreadable. We need to make the file the newest version of dxf by doing it the way Autocad expects. The way I do it is by doing a save as and overwriting the program. Can we do something similar? Another thing, can we have a check for updates button in the maintinance row? Then can we have that button check if there are any new updates in the github main branch? Then if able can it update the program from the github branch? Can we also have a button that sends sketches to this folder:I: \BAREFOOT-INSTALL\Glass Production\Sketches and a button to send dxfs to this folder: I:\BAREFOOT-INSTALL\Glass Production\Programs and then can we have a button that is to send all of itt to their designated folders? Can we put these buttons in the bottom right of the gui? Let me know what we can and can't do. Take your time and make sure all of this works. 